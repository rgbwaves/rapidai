server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all non-file routes to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
